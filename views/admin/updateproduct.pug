html
  head
    title 
    block scripts
        script(src="https://kit.fontawesome.com/3f4bdd8d17.js" crossorigin="anonymous")
    block style
        style
            link(rel="stylesheet" href="https://unpkg.com/simplebar@latest/dist/simplebar.css")
            include ../../static/design.css
            include ../../static/style.css
            include ../../static/instock.css
            include ../../static/stylesignup.css
            include ../../static/updateproducts.css
  body
    block content
        nav#navbar 
            div.logo 
                img(src="../../static/img/bg6.png", alt="Error Loading Image")
            ul.navlist
                li #[a(href="#") My Store]
            div.rightnav 
                if(currentUser)
                    ul
                        li #[a(href="/username") #{currentUser.name}]
                        li #[a(href="/changepassword") Change Password]
                        li #[a(href="/logout") Log Out]
        div.container
            div.cont
                include ../partials/sidebar.pug
            div.container1 
                h1 Update Products 
                include ../partials/messages.pug
                if(message!='' && message.includes("Update started"))
                    div.box 
                        | Update started sucessfully.Please read the following instructions: 
                        ul 
                            li Make sure you have an fast and active internet connection.
                            li To see update information go to dashboard.
                form(action="/updateproduct",method="POST")
                    input(type="submit",value="Update",class="newuserbtn")

                